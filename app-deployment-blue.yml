apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-blog-blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: post-blog
      version: blue
  template:
    metadata:
      labels:
        app: post-blog
        version: blue
    spec:
      containers:
      - name: post-blog
        image: aravindh05/green-deployment:v2
        ports:
        - containerPort: 5000
        env:
        - name: mongo_uri
          value: "mongodb://mongo-service:27017/blogdb"

